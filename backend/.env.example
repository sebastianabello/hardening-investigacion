# Tu endpoint externo (Elastic Cloud o tu cluster)
ES_BASE_URL=

# Pega tu API key (la “Encoded API Key” si Kibana te la entrega ya codificada).
ES_API_KEY=

# TLS:
# Si tu ES tiene certificado válido, deja true.
# Pon false SOLO en dev si tienes self-signed y quieres ignorar verificación.
ES_VERIFY_SSL=false

# (Opcional) Ruta a bundle/cert CA si quieres verificación con CA propia:
# ES_CA_CERT=/etc/ssl/certs/ca-bundle.crt

DATA_DIR=/tmp/app/sessions
CHUNK_SIZE=8388608
